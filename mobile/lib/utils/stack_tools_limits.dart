/// Max languages / frameworks / tools — matches server `profileLimits`.
const stackAndToolsMax = 10;

/// Per-item name length — Joi `string().max(80)`.
const stackToolNameMax = 80;
const stackToolNameMin = 1;

const stackSearchMinChars = 2;
