{
  "name": "syntax-stories",
  "private": true,
  "description": "Root scripts for the webapp / server / admin packages (each has its own package.json).",
  "scripts": {
    "lint": "npm run lint --prefix webapp && npm run lint --prefix server && npm run lint --prefix admin",
    "lint:webapp": "npm run lint --prefix webapp",
    "lint:server": "npm run lint --prefix server",
    "lint:admin": "npm run lint --prefix admin"
  }
}
