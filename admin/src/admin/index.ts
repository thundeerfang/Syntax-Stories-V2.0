/**
 * Platform admin domain: RBAC-backed management API client and types.
 * Kept separate from generic `@/lib/api` (help/trash) for clear boundaries.
 */
export * from './types';
export * from './api/management';
