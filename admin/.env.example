# Backend origin only (no /api suffix). Must match CORS (server FRONTEND_URL).
# Dev: if unset, defaults to http://localhost:7373
NEXT_PUBLIC_API_BASE_URL=http://localhost:7373

# Public product webapp (help center links)
NEXT_PUBLIC_WEBAPP_URL=http://localhost:3001

# Dedicated docs frontend (docs-webapp)
NEXT_PUBLIC_DOCS_APP_URL=http://localhost:3003
