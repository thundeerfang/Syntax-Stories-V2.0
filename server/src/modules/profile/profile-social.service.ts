/**
 * Social links are validated by Zod (`profileSection.validation` → `profileZodSchemas`).
 * This file marks the domain boundary; add URL normalization here when needed.
 */
export type ProfileSocialSectionTag = 'social';
