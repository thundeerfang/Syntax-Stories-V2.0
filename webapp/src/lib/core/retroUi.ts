/**
 * Shared retro / neo-brutalist class tokens.
 * Shadow: import `SHADOW` from `@/lib/core/shadows.js`.
 */

export { SHADOW, SHADOW_BLOCK_BUTTON, SHADOW_GHOST_HOVER, retro } from './shadows';

/** `border-2` card + block shadow. */
export const retroCard = 'retro-card';

/** `border-4` card + block shadow. */
export const retroCardLg = 'retro-card-lg';

/** Mono uppercase micro label (10px). */
export const retroLabel = 'retro-label';

/** Sort/filter dropdown trigger (bookmarks, topics, etc.). */
export const retroSortTrigger = 'retro-sort-trigger';

/** Sort/filter dropdown panel. */
export const retroDropdownPanel = 'retro-dropdown-panel';

/** Segmented control / inset panel shell. */
export const retroPanel = 'retro-panel';

/** Small metric/stat card in settings. */
export const retroMetricCard = 'retro-metric-card';
