export const achievement = {
  unlockBackdrop:
    "fixed inset-0 min-h-full min-w-full touch-none overscroll-none bg-black/65 backdrop-blur-sm pointer-events-auto",
  unlockPanel:
    "pointer-events-auto relative w-full max-w-md overflow-hidden border-2 border-border bg-card text-card-foreground shadow-[6px_6px_0_0_rgba(0,0,0,0.12)] dark:shadow-[6px_6px_0_0_rgba(0,0,0,0.45)]",
} as const;
