/**
 * Horizontal content rail aligned with the navbar inner row
 * (`Navbar`: `max-w-[90rem]` + `px-4 sm:px-6 lg:px-8`).
 * Use inside `MainLayout` so page content lines up with the menu / profile controls.
 */
export const SHELL_CONTENT_RAIL_CLASS = 'mx-auto w-full max-w-[90rem] px-4 sm:px-6 lg:px-8';

/** Wide inner column for long-form bodies (e.g. blog blocks) inside the rail. */
export const SHELL_CONTENT_MEASURE_CLASS = 'mx-auto w-full max-w-[min(88rem,100%)]';
