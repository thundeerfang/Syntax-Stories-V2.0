# Express API origin (no /api suffix). Required for CMS-backed articles.
NEXT_PUBLIC_API_BASE_URL=http://localhost:7373

# Public product app (help center links). Optional.
NEXT_PUBLIC_WEBAPP_URL=http://localhost:3001

# Site title shown in header / metadata
NEXT_PUBLIC_DOCS_SITE_NAME=Syntax Stories Docs
